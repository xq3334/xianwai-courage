# 操作系统
.DS_Store
Thumbs.db

# 编辑器
.vscode/
.idea/
*.swp
*.swo

# 日志文件
*.log
npm-debug.log*

# 临时文件
*.tmp
*.temp

# 备份文件
*.bak
*~

# Node.js (如果将来需要)
node_modules/
package-lock.json

# 构建产物 (如果将来需要)
dist/
build/

# 环境变量
.env
.env.local

# 测试覆盖率
coverage/

# 验证脚本临时输出
validate-*.log

# 图片生成的原始 PNG 暂存区：体积是成品 webp 的十几倍，不入库。
# 但本地要留着 —— generate-ending-posters.mjs 靠它判断是否跳过重新生成。
_refpng/
